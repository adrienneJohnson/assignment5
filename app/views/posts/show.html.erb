<h3><%= @post.title %></h3>
<p>by <%= @user.name %></p>
<p><%= @post.body %></p>

<p><%= link_to "Edit Post", edit_post_path(@post) %></p>

<p><%= link_to "Delete Post", post_path(@post), method: :delete, data: {confirm: "Are you sure you want to delete the post?"} %></p>